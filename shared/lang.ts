import { WHATEVER } from "./types.d.ts";

export const isUndefined = ($$1: WHATEVER): boolean => $$1 === undefined;
export const isNull = ($$1: WHATEVER): boolean => $$1 === null;
export const isNil = ($$1: WHATEVER): boolean => $$1 == null;
