/**
 * l'Element qui est l'élément racine du document (par exemple, l'élément <html> pour les documents HTML).
 */
export const $html: HTMLElement = <HTMLElement> document.documentElement;

/**
 * Retourne l'élément <body> du document courant.
 */
export const $body: HTMLBodyElement = <HTMLBodyElement> document.body;
